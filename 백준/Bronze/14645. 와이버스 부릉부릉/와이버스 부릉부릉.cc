#define _CRT_SECURE_NO_WARNINGS
#include <iostream>
using namespace std;

int main(void)
{
	int n, k;
	int a, b;
	cin >> n >> k;
	for (int i = 0; i < n; i++) {
		cin >> a >> b;
	}
	cout << "비와이" << endl;
}