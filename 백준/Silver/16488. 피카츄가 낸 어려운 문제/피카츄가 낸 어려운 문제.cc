#include <bits/stdc++.h>
using namespace std;

int main(void)
{
  ios::sync_with_stdio(0);
  cin.tie(0);

  long long n, k, res;
  cin >> n >> k;
  res = n * n * k;
  cout << res;
  return 0;
}